# 生产环境配置
# HTTP API 使用相对路径（由 Nginx /api/ 代理转发）
VITE_API_BASE_URL=/api

# WebSocket 必须使用绝对地址（浏览器不支持相对 ws:// URL）
# 注意：不要在末尾加 /ws，代码中已自动追加 /ws/projects/{id}
# 请将 server-ip 替换为服务器实际 IP 地址，端口与 Nginx 监听端口一致
VITE_WS_BASE_URL=ws://10.69.41.208:9093